class Date

instance variables
public day: nat1;
public month: nat1;
public year: nat;

operations
public Date: nat*nat*nat ==> Date
	Date(newDay,newMonth,newYear) == (
		day := newDay;
		month := newMonth;
		year := newYear;
	)
	pre newDay < 32 and newMonth < 13;
	
end Date